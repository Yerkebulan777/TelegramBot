-- ============================================================
-- Migration 002: Add ProjectName to Sessions
-- ============================================================
-- Цель: Добавить колонку ProjectName в таблицу Sessions
--       для хранения имени проекта в каждой сессии.
-- Безопасно для многократного запуска (идемпотентно).
-- ============================================================

BEGIN;

-- Шаг 1: Добавляем колонку ProjectName (IF NOT EXISTS — безопасно при повторном запуске)
ALTER TABLE Sessions
    ADD COLUMN IF NOT EXISTS ProjectName TEXT;

COMMIT;
