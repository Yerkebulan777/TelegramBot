{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/yzhumabayev/RevitBIMFusion/Docs/ResultFile.schema.json",
  "title": "ResultFile",
  "description": "Выходной result-файл BIM-Worker контракта. AddIn пишет атомарно в TaskFile.resultFilePath.",
  "type": "object",
  "required": ["status"],
  "properties": {
    "status": {
      "type": "string",
      "description": "Статус выполнения команды. Сериализуется в camelCase: done, failed, cancelled.",
      "enum": ["done", "failed", "cancelled"]
    },
    "errorMessage": {
      "type": ["string", "null"],
      "description": "Краткое сообщение об ошибке. Заполняется при status=failed. null при done.",
      "default": null
    },
    "errorDetails": {
      "type": ["string", "null"],
      "description": "Полный stack trace / диагностика. Опционально: заполняется при неожиданных исключениях. null при done и при ожидаемых сбоях.",
      "default": null
    },
    "outputFiles": {
      "type": ["string", "null"],
      "description": "Несмотря на множественное число в имени, это одна строка, не массив: ResultFile.OutputFiles в коде — string?. Для PDF/NWC — путь к единственному файлу. Для DWG (один .dwg на лист) — путь к папке экспорта, а не список файлов листов. Поле опускается из JSON при null (WhenWritingNull).",
      "default": null
    }
  },
  "examples": [
    {
      "status": "done",
      "outputFiles": "C:/Export/building.pdf"
    },
    {
      "status": "failed",
      "errorMessage": "Document is not saved"
    },
    {
      "status": "failed",
      "errorMessage": "AddIn failed: Object reference not set to an instance of an object.",
      "errorDetails": "System.NullReferenceException: ...\n   at WorkerBridge.Commands.WorkerCommandHandler.Execute ..."
    }
  ]
}
